#!/usr/bin/with-contenv sh

# set crontab
cat /config/rclone_cron
crontab -u abc /config/rclone_cron

# create empty config file
touch /config/rclone.conf

# permissions
chown abc:abc /config/rclone.conf
chmod 777 /var/lock